<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pakmall.mapper.BoardMapper">

<!-- 게시판 리스트 -->
<select id="getBoardList" resultType="com.pakmall.domain.BoardVO" >
	SELECT BD_NUM
		, BD_TITLE
		, BD_CONTENT
		, MEM_ID
		, TO_CHAR(BD_DATE_REG, 'yyyy-mm-dd hh24:mi:ss') as BD_DATE_REG
	FROM BOARD_TBL
	ORDER BY BD_NUM DESC
</select>

<!-- 게시판 리스트 상세 -->
<select id="board_detail" resultType="com.pakmall.domain.BoardVO">
	SELECT bd_num
			, bd_title
			, bd_content
			, mem_id
			, bd_date_reg
	    FROM BOARD_TBL
	    WHERE bd_num = #{bd_num}
</select>

<!-- 게시글 쓰기 -->
<insert id="board_register" >
	INSERT INTO BOARD_TBL(BD_NUM, BD_TITLE, BD_CONTENT, MEM_ID, BD_DATE_REG)
	VALUES (
		  SEQ_BOARD_TBL.NEXTVAL
		, #{bd_title}, #{bd_content}
		, #{mem_id}
		, SYSDATE)
</insert>

</mapper>